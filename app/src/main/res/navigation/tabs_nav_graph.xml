<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/tabs_nav_graph"
    app:startDestination="@id/home_nav_graph">
    <action android:id="@+id/action_to_profile_nav_graph"
        app:destination="@id/profile_nav_graph"/>
    <action
        android:id="@+id/action_to_profileFragment"
        app:destination="@id/profileFragment" />

    <include app:graph="@navigation/home_nav_graph" />
    <include app:graph="@navigation/favourite_nav_graph" />
    <include app:graph="@navigation/cart_nav_graph" />
    <include app:graph="@navigation/messenger_nav_graph" />
    <include app:graph="@navigation/profile_nav_graph" />
</navigation>